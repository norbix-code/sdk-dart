name: norbix_api
description: Official Dart SDK for the project-scoped Norbix API (api.norbix.ai). Resource-style client with env-based config.
version: 1.1.0
homepage: https://norbix.ai
repository: https://github.com/norbix-dev/norbix-dart
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  norbix_core: any

dev_dependencies:
  http: ^1.2.0
  lints: ^5.0.0
  test: ^1.25.0
