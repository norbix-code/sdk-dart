name: _norbix_dart_workspace
publish_to: none
description: Workspace root for the Norbix Dart SDKs (norbix_core, norbix_api, norbix_hub).
version: 0.0.0

environment:
  sdk: ">=3.6.0 <4.0.0"

workspace:
  - packages/norbix_core
  - packages/norbix_api
  - packages/norbix_hub
